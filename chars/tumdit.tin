#alias {g} {
  #if { "%0" == "" } {
    cast f 'dispel evil' $target; #echo {cast f 'dispel evil' $target}
  };
  #else {
    cast f 'dispel evil' %0; #echo {cast f 'dispel evil' %0}
  };
}

#alias {h} {
  #if { "%0" == "" } {
    cast 'dispel evil' $target; #echo {cast 'dispel evil' $target}
  };
  #else {
    cast 'dispel evil' %0; #echo {cast 'dispel evil' %0}
  };
}
