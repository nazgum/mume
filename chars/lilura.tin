#var {bag} {pack}
#var {basher} {followers}
#var {protector} {followers}

#alias {basher} {
  #if { "%0" == "" } {
    #echo {basher set to: <049>$basher<099>}
  };
  #else {
    #var {basher} {%0};
    #echo {basher set to: <049>$basher<099>}
  };
}

#alias {protector} {
  #if { "%0" == "" } {
    #echo {protector set to: <049>$protector<099>}
  };
  #else {
    #var {protector} {%0};
    #echo {protector set to: <049>$protector<099>}
  };
}

#alias {mm} {
  #if { "%0" == "" } {
    cast 'magic missile' $target; #echo {cast 'magic missile' $target}
  };
  #else {
    cast 'magic missile' %0; #echo {cast 'magic missile' %0}
  };
}
#alias {g} {mm}
#alias {h} {mm}

#alias {b} {
  #if { "%0" == "" } {
    cast 'charm' $target; #echo {cast 'charm' $target}
  };
  #else {
    cast 'charm' %0; #echo {cast 'charm' %0}
  };
}


#alias {pipe} {get pipe $bag; wield pipe; rest; smoke pipe; st; wield axe; put pipe $bag }
#alias {remp} {remove ring; get emerald-ring $bag; wear emerald-ring; put ring $bag; use emerald-ring}
#alias {cc} {remove ring; get garnet-ring $bag; wear garnet-ring; put ring $bag; use garnet-ring}

#alias {1h} {wield 1; remove buckler; wear tower}
#alias {2h} {remove tower; wield 2; wear buckler}
#alias {cloak} {remove fur; get cloak $bag; wear cloak; put fur $bag}
#alias {fur} {remove cloak; get fur $bag; wear fur; put cloak $bag}

#alias {^1$} {order $basher bash $target; #echo {order $basher bash $target}}
#alias {^2$} {of kill $target; #echo {of kill $target}}
#alias {^3$} {of assist}
#alias {^4$} {order $basher bash; #echo {order $basher bash}}
#alias {^5$} {order $protector rescue lilura; #echo {order $protector rescue lilura}}
