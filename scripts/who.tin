#var {players}

#gag {^%1 allies and visible Ainur on.}
#gag {^%1 allies on.}

#action {^%1 allies and visible Ainur on.} {
  who_count %1;
}

#action {^%1 allies on.} {
  who_count %1;
}

#alias {who_count} {
  #script {ruby scripts/who.rb};
  
  #echo {};
  #echo {%1 visible players online.};
  #echo {$players accounts logged in recently.};
}
